// INITIALIZE STATE

const initialState = {
};


// REDUCER

export const FetchZipCodesReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};